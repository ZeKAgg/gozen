//! Gozen CLI library. The main entry point is the `gozen` binary (main.rs).
//! This lib exists so that workspace integration tests can depend on the package
//! and get `CARGO_BIN_EXE_gozen` set when running tests.
