extends Node

func example():
	var skill = Skill.new()
	skill.id = dict.get("id", -1)
	skill.row = dict.get("row", 0)
	skill.col = dict.get("col", 0)
	skill.type = dict.get("type", Enums.SkillType.DAMAGE)
	var arr = [1, 2, 3, 4]
	var result = clamp(relic+kt+total_kt, 0, 9999999)
